things todo for version 0.7:
	- evergreens
		* bugfixing
		* get other not implemented ftp rfc's and put them in doc/
		* improve user methods for userbility (chefxp,chefxp4jboss,...)

	- new constructor with URL
	- ssh ftp
	- connection states
	- CPSV (look in forum "help" for details) ??
	- client side encryption using FUSE ??
	- add SOCKS5 proxy support
	- Add handling for TYPE A (download / upload)
	- downloadByteBuffer
		* byte buffer handling for resume
		* byte buffer handling for getting data
	- uploadStream ??
	- uploadByteBuffer ??
	